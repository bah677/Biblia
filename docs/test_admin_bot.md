# Тестирование Admin Bot (@SuperAdmin_MirOn_bot)

## ✅ Чек-лист команд для тестирования:

### 1. Основные команды

- [ ] `/start` - Приветствие и список команд
  - Ожидается: Приветственное сообщение с описанием всех команд
  
- [ ] `/stats` - Общая статистика
  - Ожидается: Количество пользователей, активных за 30 дней, всего сообщений
  
- [ ] `/token_stats` - Статистика токенов (опционально с параметром дней)
  - Ожидается: Общее количество токенов, prompt, completion, уникальных пользователей
  - Пример: `/token_stats 7`
  
- [ ] `/token_leaderboard` - Топ пользователей по токенам
  - Ожидается: Список топ-10 пользователей с количеством токенов
  - Пример: `/token_leaderboard 7`

### 2. Работа с тикетами

- [ ] `/tickets` - Список всех активных тикетов
  - Ожидается: Список открытых и в работе тикетов с номерами
  
- [ ] `/my_tickets` - Мои взятые тикеты
  - Ожидается: Список тикетов, взятых текущим админом
  
- [ ] `/viewTKT12345678` - Просмотр конкретного тикета
  - Ожидается: Детали тикета с кнопками действий
  
- [ ] **Deep link из группы** - Клик по ссылке из уведомления
  - Ожидается: Открывается бот с деталями тикета

### 3. Действия с тикетом

- [ ] **Кнопка "Взять в работу"**
  - Ожидается: Тикет переходит в статус "in_progress", сообщение удаляется из группы
  
- [ ] **Кнопка "Ответить"**
  - Ожидается: Бот просит написать ответ, сохраняет его
  
- [ ] **Кнопка "Закрыть"**
  - Ожидается: Тикет закрывается, статус меняется на "closed"

### 4. Управление админами (только для суперадмина ID: 304631563)

- [ ] `/add_admin <user_id>` - Добавить админа
  - Ожидается: Админ добавлен в базу
  - Пример: `/add_admin 123456789`
  
- [ ] `/remove_admin <user_id>` - Удалить админа
  - Ожидается: Админ удален из базы
  - Пример: `/remove_admin 123456789`
  
- [ ] `/list_admins` - Список всех админов
  - Ожидается: Список админов с username и датой добавления

### 5. Уведомления в группе

- [ ] **Создание тикета в User Bot**
  - Ожидается: Уведомление появляется в группе MironAdmin в топике #2
  - Проверить: Имя, фамилия, username пользователя отображаются
  - Проверить: Превью ссылки отключено
  - Проверить: Deep link работает

---

## 🔧 Известные исправления:

1. ✅ Убран дефис из номера тикета: `TKT12345678` (вместо `TKT-12345678`)
2. ✅ Добавлена команда `/view` для просмотра тикетов
3. ✅ Исправлено поле `user_message` в запросах
4. ✅ Добавлены методы работы с тикетами в `TicketStorage`
5. ✅ Отключено превью ссылки в уведомлениях группы
6. ✅ Добавлены имя и фамилия пользователя в уведомления

---

## 📊 Проверка базы данных:

```sql
-- Проверка тикетов
SELECT ticket_number, status, topic, user_id, admin_id FROM support_tickets ORDER BY created_at DESC LIMIT 5;

-- Проверка админов
SELECT user_id, username, first_name, is_active FROM admins;

-- Проверка содержимого для кнопок
SELECT category, COUNT(*) FROM bot_content GROUP BY category;
```

---

## ⚠️ Что нужно проверить особенно тщательно:

1. **Deep link работает** - клик по ссылке из группы открывает тикет
2. **Взятие тикета** - сообщение удаляется из группы, статус меняется
3. **Ответ на тикет** - текст сохраняется в базу
4. **Закрытие тикета** - статус меняется на closed
5. **Команда /viewTKT...** - показывает детали тикета
6. **Статистика** - все команды работают без ошибок

---

## 🐛 Как тестировать:

1. Создайте тикет в @SlovaBoga_bot через `/support`
2. Проверьте что уведомление пришло в группу
3. Откройте @SuperAdmin_MirOn_bot
4. Попробуйте все команды из чек-листа
5. Проверьте логи: `tail -f /var/log/supervisor/admin_bot.out.log`
