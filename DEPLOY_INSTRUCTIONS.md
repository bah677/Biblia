# 🚀 Инструкция по развертыванию User Bot & Admin Bot

## Текущее состояние
- ✅ PostgreSQL установлен и настроен
- ✅ База данных `telegram_bot` создана
- ✅ Все таблицы и миграции выполнены
- ✅ Supervisor конфигурация готова
- ✅ Оба бота готовы к запуску

## 📋 Пошаговая инструкция

### Шаг 1: Остановить старый бот (если запущен)

Сначала нужно найти и остановить старый бот, чтобы избежать конфликтов:

```bash
# Проверяем процессы с ботом
ps aux | grep python | grep -i bot

# Если найден процесс, останавливаем его
kill <PID>

# Или если бот в screen/tmux
screen -ls
# screen -r <session_name> и нажать Ctrl+C
```

### Шаг 2: Проверить статус PostgreSQL

```bash
# Проверить что PostgreSQL запущен
service postgresql status

# Если не запущен:
service postgresql start
```

### Шаг 3: Проверить supervisor

```bash
# Проверить статус всех сервисов
supervisorctl status

# Должны быть остановлены оба бота:
# bots:user_bot    STOPPED
# bots:admin_bot   STOPPED
```

### Шаг 4: Запустить User Bot

```bash
# Запускаем основной бот для пользователей
supervisorctl start bots:user_bot

# Проверяем статус
supervisorctl status bots:user_bot

# Смотрим логи (последние 50 строк)
tail -50 /var/log/supervisor/user_bot.out.log
```

**Ожидаемый результат:**
```
✅ BOT STARTING
✅ Configuration validated successfully
✅ Bot dependencies initialized
✅ Start polling for bot @SlovaBoga_bot
```

### Шаг 5: Запустить Admin Bot

```bash
# Запускаем админский бот
supervisorctl start bots:admin_bot

# Проверяем статус
supervisorctl status bots:admin_bot

# Смотрим логи
tail -50 /var/log/supervisor/admin_bot.out.log
```

**Ожидаемый результат:**
```
✅ ADMIN BOT STARTING
✅ Configuration validated successfully
✅ Admin bot dependencies initialized
✅ Starting admin bot polling...
```

### Шаг 6: Проверить что оба бота работают

```bash
# Проверяем статус обоих ботов
supervisorctl status bots:*
```

**Должно быть:**
```
bots:admin_bot    RUNNING   pid 1234, uptime 0:00:15
bots:user_bot     RUNNING   pid 5678, uptime 0:00:30
```

### Шаг 7: Тестирование User Bot

1. Откройте Telegram
2. Найдите бота `@SlovaBoga_bot`
3. Отправьте `/start`
4. Проверьте команды: `/more`, `/support`, `/mytickets`, `/affiliate`

### Шаг 8: Создать тестовый тикет

```bash
# В User Bot:
1. Отправьте /support
2. Выберите тему
3. Опишите проблему
4. Получите номер тикета (например: TKT20251113001)
```

### Шаг 9: Добавить себя как админа в Admin Bot

Сначала нужно добавить ваш Telegram ID как админа:

```bash
# Подключаемся к базе
sudo -u postgres psql -d telegram_bot

# Добавляем вас как админа (ваш ID: 304631563 - уже есть как SUPER_ADMIN)
INSERT INTO admins (user_id, username, first_name, added_by_admin_id, is_active, added_at)
VALUES (304631563, 'your_username', 'Ваше Имя', 304631563, true, NOW())
ON CONFLICT (user_id) DO NOTHING;

# Выходим
\q
```

### Шаг 10: Тестирование Admin Bot

1. Найдите админского бота в Telegram (имя будет в логах)
2. Отправьте `/start`
3. Проверьте команды:
   - `/stats` - статистика
   - `/tickets` - список тикетов
   - `/list_admins` - список админов

### Шаг 11: Настроить админский канал

**Важно!** Админский бот должен быть добавлен в канал `https://t.me/+WLn4SNrLCjRiMDIy` как администратор.

1. Откройте канал в Telegram
2. Нажмите "⋮" → "Управление каналом"
3. "Администраторы" → "Добавить администратора"
4. Найдите админского бота и добавьте его
5. Дайте права: "Публикация сообщений", "Редактирование сообщений", "Удаление сообщений"

### Шаг 12: Создать топики в канале (опционально)

Если хотите использовать топики для разных типов тикетов:

1. Откройте канал
2. Включите "Темы" (Topics) в настройках канала
3. Создайте топики: "Техподдержка", "Вопросы", "Баги" и т.д.

## 🔍 Проверка работы системы

### Полный workflow тикета:

1. **Пользователь создает тикет** в User Bot (`/support`)
2. **User Bot сохраняет** тикет в БД
3. **Admin Bot постит** уведомление в админский канал с кнопкой
4. **Админ кликает** по ссылке → открывается Admin Bot
5. **Админ берет тикет** → сообщение удаляется из канала
6. **Админ отвечает** → ответ сохраняется
7. **Админ закрывает** тикет

## 📊 Мониторинг

### Просмотр логов в реальном времени:

```bash
# User Bot
tail -f /var/log/supervisor/user_bot.out.log

# Admin Bot
tail -f /var/log/supervisor/admin_bot.out.log

# Оба одновременно
tail -f /var/log/supervisor/user_bot.out.log /var/log/supervisor/admin_bot.out.log
```

### Просмотр ошибок:

```bash
# User Bot ошибки
tail -f /var/log/supervisor/user_bot.err.log

# Admin Bot ошибки
tail -f /var/log/supervisor/admin_bot.err.log
```

## 🛠️ Управление ботами

### Команды supervisor:

```bash
# Статус всех ботов
supervisorctl status bots:*

# Перезапуск User Bot
supervisorctl restart bots:user_bot

# Перезапуск Admin Bot
supervisorctl restart bots:admin_bot

# Перезапуск обоих
supervisorctl restart bots:*

# Остановка
supervisorctl stop bots:*

# Запуск
supervisorctl start bots:*
```

## ⚠️ Возможные проблемы

### 1. Конфликт с другим ботом

**Ошибка:** `TelegramConflictError: terminated by other getUpdates request`

**Решение:**
```bash
# Найти и остановить старый процесс
ps aux | grep python | grep bot
kill <PID>

# Перезапустить бота
supervisorctl restart bots:user_bot
```

### 2. Ошибка подключения к базе

**Ошибка:** `Connection refused` или `Connection timeout`

**Решение:**
```bash
# Проверить PostgreSQL
service postgresql status
service postgresql start

# Проверить подключение
psql -U bot_user -d telegram_bot -h localhost
```

### 3. Бот не отвечает

**Решение:**
```bash
# Проверить логи
tail -50 /var/log/supervisor/user_bot.err.log

# Перезапустить
supervisorctl restart bots:user_bot
```

## 📞 Контакты ботов

- **User Bot:** `@SlovaBoga_bot` (Token: 7404722403...)
- **Admin Bot:** (узнать username из логов после запуска)
- **Админский канал:** `https://t.me/+WLn4SNrLCjRiMDIy`

## ✅ Чеклист развертывания

- [ ] PostgreSQL запущен
- [ ] Старый бот остановлен
- [ ] User Bot запущен и работает
- [ ] Admin Bot запущен и работает
- [ ] Тестовый тикет создан в User Bot
- [ ] Админ добавлен в базу
- [ ] Admin Bot добавлен в канал как администратор
- [ ] Workflow тикета протестирован
- [ ] Логи проверены на ошибки

## 🎉 Готово!

Оба бота развернуты и готовы к работе. Система тикетов настроена и функционирует.
