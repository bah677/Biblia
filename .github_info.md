# GitHub Repository Information

## 📦 Содержимое репозитория

### ✅ Что включено

#### Исходный код
- ✅ `admin_bot/` - Административный бот
- ✅ `user_bot/` - Пользовательский бот  
- ✅ `shared/` - Общие модули (storage, models)

#### База данных
- ✅ `database/` - SQL скрипты инициализации
- ✅ Автоматический скрипт установки `setup_database.sh`
- ✅ Все таблицы с правильными правами доступа

#### Конфигурация
- ✅ `.env.example` - Шаблон переменных окружения (в корне)
- ✅ `supervisor_bots.conf` - Конфигурация Supervisor
- ✅ `requirements.txt` в каждом боте

#### Документация
- ✅ `README.md` - Основная информация
- ✅ `DEPLOYMENT.md` - Подробное развертывание
- ✅ `QUICK_DEPLOY.md` - Быстрый чеклист
- ✅ `ENV_CONFIGURATION.md` - Конфигурация переменных
- ✅ `CHANGELOG.md` - История изменений
- ✅ `docs/` - Дополнительные гайды

### ❌ Что исключено (.gitignore)

#### Секретные данные
- ❌ `.env` - Переменные окружения с токенами
- ❌ Любые файлы с расширением `.env`

#### Логи и кеш
- ❌ `logs/` - Лог файлы
- ❌ `__pycache__/` - Python кеш
- ❌ `*.pyc`, `*.pyo` - Скомпилированные файлы
- ❌ `*.log` - Лог файлы

#### Виртуальное окружение
- ❌ `venv/`, `env/`, `ENV/` - Виртуальные окружения Python

#### IDE настройки
- ❌ `.vscode/`, `.idea/` - Настройки редакторов
- ❌ `*.swp`, `*.swo` - Временные файлы

## 🔐 Безопасность

### GitHub Secret Scanning

Репозиторий защищен от случайной загрузки секретов:

1. **`.gitignore`** - Исключает `.env` файлы
2. **GitHub Push Protection** - Автоматически блокирует push с токенами
3. **Secret Scanning** - Сканирует историю на наличие секретов

### Что делать при случайной загрузке секрета

1. **Немедленно ротируйте ключи:**
   - Telegram Bot Token: Через @BotFather → /revoke
   - OpenAI API Key: platform.openai.com → Revoke key
   - Database Password: Смените пароль в PostgreSQL

2. **Очистите историю Git:**
   ```bash
   # ВНИМАНИЕ: Это переписывает историю!
   git filter-branch --force --index-filter \
   "git rm --cached --ignore-unmatch .env" \
   --prune-empty --tag-name-filter cat -- --all
   ```

3. **Force push:**
   ```bash
   git push origin --force --all
   ```

## 📊 Статистика репозитория

### Структура кода
- **Python файлов:** ~40
- **SQL скриптов:** 3
- **Markdown документов:** 10+
- **Конфигурационных файлов:** 5

### Размеры
- **Без зависимостей:** ~2 MB
- **С venv:** ~500 MB (исключен из Git)

### Языки
- Python: 90%
- SQL: 5%
- Shell: 3%
- Markdown: 2%

## 🚀 Развертывание из GitHub

### Быстрый старт

```bash
# 1. Клонирование
git clone https://github.com/bah677/Biblia.git
cd Biblia

# 2. Конфигурация
cp .env.example .env
nano .env  # Заполните переменные

# 3. База данных
cd database && sudo ./setup_database.sh && cd ..

# 4. Зависимости
cd user_bot && pip install -r requirements.txt && cd ..
cd admin_bot && pip install -r requirements.txt && cd ..

# 5. Запуск
python user_bot/main.py &
python admin_bot/main.py &
```

### Через Docker (будущее)

```bash
# Планируется добавить
docker-compose up -d
```

## 🔄 Обновление проекта

### Получение последних изменений

```bash
cd /path/to/Biblia
git pull origin main
```

### Применение обновлений

```bash
# 1. Проверьте CHANGELOG.md на breaking changes

# 2. Обновите зависимости
cd user_bot && pip install -r requirements.txt && cd ..
cd admin_bot && pip install -r requirements.txt && cd ..

# 3. Примените миграции БД (если есть)
sudo -u postgres psql -d telegram_bot -f database/migrations/xxx.sql

# 4. Перезапустите боты
sudo supervisorctl restart all
```

## 📝 Contributing (если планируется)

### Как внести вклад

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменений (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

### Правила кода

- Следуйте PEP 8 для Python
- Добавляйте docstrings к функциям
- Обновляйте документацию
- Проверяйте линтером перед коммитом

## 🏷️ Версионирование

Проект использует [Semantic Versioning](https://semver.org/):

- **MAJOR** (2.x.x) - Несовместимые изменения API
- **MINOR** (x.1.x) - Новый функционал (обратно совместимый)
- **PATCH** (x.x.1) - Исправление багов

## 📞 Контакты

- **GitHub:** [@bah677](https://github.com/bah677)
- **Repository:** [github.com/bah677/Biblia](https://github.com/bah677/Biblia)

## 📄 Лицензия

Проект для личного использования. Лицензия не определена.

---

**Последнее обновление:** 2025-11-14
**Версия:** 2.0.0
